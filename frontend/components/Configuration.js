import React from "react";
import Navigation from "./Navigation";

const Configuration = () => {

    return (
        <div>
            <Navigation/>
            <p>On the configuration page</p>
        </div>
    );
}

export default Configuration;